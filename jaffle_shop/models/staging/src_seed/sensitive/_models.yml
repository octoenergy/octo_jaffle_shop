version: 2

models:
  - name: stg_customers_pii
    meta:
      owner: 'jaffle.mcjaffleson@gmail.com'
    description: >
      Staging layer for customers' source data.
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null
      - name: first_name
        meta:
          sensitive: true
      - name: last_name
        meta:
          sensitive: true
